package com.dicoding.kulinerku.ui.screen.recomendations

import androidx.lifecycle.ViewModel
import com.dicoding.kulinerku.data.UserRepository

class RecomendationsViewModel(private val repository: UserRepository) : ViewModel() {
}